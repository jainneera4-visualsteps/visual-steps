name: Update Copilot Instructions 🔄

on:
  push:
    branches:
      - main # Runs whenever code is pushed or merged into the main branch

jobs:
  update-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository 📥
        uses: actions/checkout@v4

      # This step ensures your instructions file stays noted when code changes
      - name: Log Action Trigger 📝
        run: |
          echo "Main branch updated! Checking for documentation adjustments..."